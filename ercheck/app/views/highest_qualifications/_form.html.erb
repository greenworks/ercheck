<%= form_for(@highest_qualification) do |f| %>
  <% if @highest_qualification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@highest_qualification.errors.count, "error") %> prohibited this highest_qualification from being saved:</h2>

      <ul>
      <% @highest_qualification.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
